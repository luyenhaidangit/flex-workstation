# VS Code personal settings
.vscode/launch.json
.vscode/*.log

# Local environment files
.env
.env.*
!.env.example
.claude/settings.local.json

# OS and editor noise
.DS_Store
Thumbs.db
*.swp
*.swo

# Logs and temporary files
*.log
*.tmp
*.temp

# .agents/skills/ is the tracked source of truth for shared skills.
# .claude/skills/ are junctions to .agents/skills, created by bootstrap (not tracked).
.claude/skills/

# Nested Flex repositories cloned by workstation sync
/flex-agent-service/
/flex-agents/
/flex-auth-service/
/flex-api-gateway/
/flex-database/
/flex-microfrontend/
/flex-environment/
/flex-exchange-service/
